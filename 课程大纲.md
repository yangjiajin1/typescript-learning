# TypeScript 学习课程大纲(目标 B 档)

> 共识确认于 2026-08-07。每日一课(20-40 分钟),可加量。每课结构:**讲解 → 练习(用户写,Claude 审)→ 自测**。练习代码放 `lessons/NN-名称/`。
> 每课结束,Claude 必须更新 `progress.md`(进度 + 错题本 + any 记录)。

## 阶段一:看懂类型(第 1-6 课)——先治好"看不懂"

### 第 1 课:TS 是什么——类型注解与第一次类型检查
- **目标**:能解释 TS 与 JS 的关系(编译期检查、运行时不变);会写变量/参数/返回值注解;会看类型报错
- **要点**:TS 是 JS 的超集;注解语法 `: type`;`tsc` 报错 vs 代码仍能运行;`npm run check` 与 `npm run <文件>` 的用法
- **练习**:`lessons/01-认识类型/exercise.ts` —— 给一段无注解的函数补上注解;故意写错类型,运行 `npm run check` 观察报错文案,抄写报错并翻译成大白话
- **自测**:解释"为什么 TS 报错但 JS 还是跑起来了";说出 3 种注解位置
- **文档**:https://www.typescriptlang.org/docs/handbook/typescript-from-scratch.html

### 第 2 课:类型推断——TS 怎么猜你的类型
- **目标**:能预判简单代码的推断结果;知道何时该显式注解、何时该省略
- **要点**:推断规则(变量初始化、函数返回值);"注解用于形状,推断用于实现";显式注解的必要场景(函数参数、空数组)
- **练习**:`lessons/02-类型推断/exercise.ts` —— 预判 8 段代码的推断结果,再运行验证;找出函数参数不注解时报错的原因
- **自测**:说出"该写注解"与"可以省略"各两个场景
- **文档**:https://www.typescriptlang.org/docs/handbook/type-inference.html

### 第 3 课:原始类型与字面量类型
- **目标**:熟悉 `string/number/boolean/null/undefined/bigint/symbol`;会用字面量类型与 `as const`
- **要点**:字面量类型 `"draft"`、`42`;`const` 与字面量的关系;`as const` 冻结对象属性
- **练习**:`lessons/03-原始类型/exercise.ts` —— 定义订单状态/角色的字面量类型;用 `as const` 重构一个配置对象并体会推断变化
- **自测**:`let x = "a"` 与 `const y = "a"` 的推断差异
- **文档**:https://www.typescriptlang.org/docs/handbook/2/everyday-types.html

### 第 4 课:interface 与 type——对象类型
- **目标**:能设计对象类型;会用可选属性 `?`、`readonly`;知道 interface 与 type 的取舍
- **要点**:对象类型语法;`?` 与 `readonly`;interface 的 extends、合并;type 的联合/交叉能力;取舍共识(能用 interface 用 interface,需要联合/交叉用 type)
- **练习**:`lessons/04-对象类型/exercise.ts` —— 定义"电影""订单"等领域对象,加可选/只读属性;同义改写 interface ↔ type
- **自测**:说出 interface 的两种 type 做不到的能力
- **文档**:https://www.typescriptlang.org/docs/handbook/2/objects.html

### 第 5 课:数组与元组
- **目标**:会用 `T[]`/`Array<T>`/`ReadonlyArray`;会写元组;理解 `noUncheckedIndexedAccess` 带来的检查
- **要点**:数组类型;元组 `[string, number]`、可选元组元素、命名元组;越界访问报错(本仓库开了 `noUncheckedIndexedAccess`)
- **练习**:`lessons/05-数组元组/exercise.ts` —— 坐标/CSV 行元组;安全读取数组元素的函数
- **自测**:元组与数组的区别;`arr[0]` 为何可能是 `undefined`
- **文档**:https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#arrays

### 第 6 课:null/undefined 与严格空检查
- **目标**:理解 `strictNullChecks`;会用可选链 `?.`、空值合并 `??`;知道"null 处理是类型设计的一部分"
- **要点**:`strictNullChecks` 的意义;`?.` 链式访问;`??` 与 `||` 的区别;`!` 非空断言的代价(慎用)
- **练习**:`lessons/06-空值处理/exercise.ts` —— 把一段充满 `if (x && x.y && x.y.z)` 的代码用 `?.` 改写;处理"可能为 null 的用户输入"
- **自测**:`0 ?? 10` 与 `0 || 10` 分别是什么;何时不该用 `!`
- **文档**:https://www.typescriptlang.org/docs/handbook/2/narrowing.html#nullish-coalescing

## 阶段二:类型系统核心(第 7-14 课)

### 第 7 课:联合类型——"或"的思维
- **目标**:能写 `A | B`;理解联合在赋值与读取时的表现(只能访问共有成员)
- **要点**:联合语法;数组元素联合;联合 + 窄化预览(下两课正式学)
- **练习**:`lessons/07-联合类型/exercise.ts` —— `ID: string | number` 场景;尝试读取联合的独有属性并观察报错
- **自测**:为什么联合类型只能访问"两者都有"的属性
- **文档**:https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#union-types

### 第 8 课:交叉类型与接口扩展
- **目标**:会用 `&` 组合类型;知道 `extends` 与 `&` 的适用场景
- **要点**:`A & B` 语义;"同时拥有";`&` 与 interface extends 的取舍(primitive 联合无法 extends,用 &;对象扩展推荐 extends)
- **练习**:`lessons/08-交叉类型/exercise.ts` —— 组合"可命名 + 可计时 + 可序列化"能力;对比用 interface extends 的写法
- **自测**:`{a: 1} & {b: 2}` 与 `interface` 继承的差异
- **文档**:https://www.typescriptlang.org/docs/handbook/2/objects.html#intersection-types

### 第 9 课:类型守卫与窄化(narrowing)
- **目标**:能用 `typeof`/`instanceof`/`in`/字面量判断收窄类型;写出安全的窄化函数
- **要点**:窄化是"在一个作用域内让类型变精确";四种守卫;常见窄化陷阱(`typeof null === "object"`)
- **练习**:`lessons/09-类型守卫/exercise.ts` —— 写 `formatInput(input: string | number | Date)`;用 `in` 处理"猫/狗"联合对象
- **自测**:说出 3 种类型守卫;`typeof null` 是什么
- **文档**:https://www.typescriptlang.org/docs/handbook/2/narrowing.html

### 第 10 课:可辨识联合与穷尽检查
- **目标**:会用 discriminated union 建模状态机;用 `never` 做穷尽检查
- **要点**:判别字段(通常 `type`/`kind`);switch 收窄;`default: never` 让未来新分支无处遁形
- **练习**:`lessons/10-可辨识联合/exercise.ts` —— 建模"订单状态:待支付/已支付/已发货/已取消"渲染函数;故意漏一个分支看报错
- **自测**:为什么判别字段必须是字面量类型
- **文档**:https://www.typescriptlang.org/docs/handbook/2/narrowing.html#discriminated-unions

### 第 11 课:any / unknown / never 三兄弟
- **目标**:能解释三者差异;理解 why avoid `any`;会用 `unknown` 并收窄它
- **要点**:`any` 关闭一切检查(危险);`unknown` 要求先收窄再使用(安全);`never` 表示不可能的值(穷尽/兜底);三者的使用决策
- **练习**:`lessons/11-三兄弟/exercise.ts` —— 把 5 处 `any` 改写成 `unknown` + 收窄;解释为什么 `never` 出现在函数底部
- **自测**:说出 `any` 的三种危害;`unknown` 在什么时候出现
- **文档**:https://www.typescriptlang.org/docs/handbook/2/functions.html#unknown

### 第 12 课:泛型入门
- **目标**:会写带泛型参数的函数;理解类型参数推断;会用 `extends` 约束
- **要点**:`function f<T>(x: T): T` 的思维模式(占位符,调用时确定);推断 vs 显式指定;约束 `T extends SomeShape`
- **练习**:`lessons/12-泛型入门/exercise.ts` —— 写 `first`, 写 `identity`;带约束的 `pluck(obj, key)`
- **自测**:泛型与 `any` 的本质区别
- **文档**:https://www.typescriptlang.org/docs/handbook/2/generics.html

### 第 13 课:泛型进阶——接口、多参数、默认值
- **目标**:会写泛型接口/泛型类;多类型参数;默认类型参数
- **要点**:`interface Box<T>`;`<K, V>` 多参数;`T = Default`;泛型约束的级联
- **练习**:`lessons/13-泛型进阶/exercise.ts` —— 泛型缓存 `Cache<K, V>`;泛型键值对工具
- **自测**:默认类型参数的使用场景
- **文档**:同上

### 第 14 课:泛型实战 + 阶段复盘
- **目标**:用泛型封装一个"请求函数"的类型骨架;回顾错题本
- **要点**:`fetchData<T>(url): Promise<T>` 的完整设计;错误返回类型设计(Result 模式:成功/失败联合)
- **练习**:`lessons/14-泛型实战/exercise.ts` —— 设计 `Result<T, E>` + `request<T>()` 的类型(不真发请求);完成阶段复盘(翻错题本)
- **自测**:`Result<T,E>` 为什么用联合而非 any
- **文档**:无(综合应用)

## 阶段三:进阶(第 15-20 课)——学完够用 90% 工作场景

### 第 15 课:索引签名、Record 与 keyof
- **目标**:会写索引签名;会用 `Record<K, V>`;会用 `keyof`
- **要点**:`[key: string]: T` 的语义与坑(`noUncheckedIndexedAccess` 下读值可能是 undefined);`Record`;`keyof T` 得到键的联合;`keyof` 与泛型的组合
- **练习**:`lessons/15-索引与keyof/exercise.ts` —— 用 Record 重构配置表;`getValue<T, K extends keyof T>` 安全取值
- **自测**:索引签名与 interface 属性的区别;`keyof {a:1,b:2}` 是什么
- **文档**:https://www.typescriptlang.org/docs/handbook/2/objects.html#index-signatures

### 第 16 课:映射类型与工具类型(上)——Partial/Required/Readonly/Pick/Omit
- **目标**:会用并理解这五个工具类型;能看懂映射类型语法
- **要点**:`[K in keyof T]` 映射语法;`Partial<T>` 源码逐行解读;Pick/Omit 的语义区别
- **练习**:`lessons/16-工具类型上/exercise.ts` —— 手写 Partial/Required/Readonly;用 Pick/Omit 裁剪类型
- **自测**:映射类型语法 `[K in keyof T]` 每个部分的作用
- **文档**:https://www.typescriptlang.org/docs/handbook/2/mapped-types.html

### 第 17 课:工具类型(下)——ReturnType/Parameters/Exclude/Extract/NonNullable
- **目标**:会用函数工具类型与集合工具类型;能从第三方函数提取类型
- **要点**:`ReturnType`/`Parameters`/`InstanceType`;`Exclude`/`Extract`/`NonNullable`;使用场景(不手写重复的接口,从函数"借"类型)
- **练习**:`lessons/17-工具类型下/exercise.ts` —— 从一段他人代码的函数提取参数/返回类型;用 Exclude 移除联合成员
- **自测**:`Exclude<'a'|'b','a'>` 是什么;为什么能直接从函数类型取 ReturnType
- **文档**:https://www.typescriptlang.org/docs/handbook/utility-types.html

### 第 18 课:条件类型与 infer
- **目标**:能读懂条件类型;会用 `infer` 提取类型(看懂 ReturnType 源码)
- **要点**:`T extends U ? X : Y`;`infer` 只能出现在 extends 位置;逐行解读 `ReturnType<T>` 源码;`Promise<T>` 拆包(Unwrap)
- **练习**:`lessons/18-条件类型/exercise.ts` —— 手写 `MyReturnType`;写 `UnwrapPromise<T>`;`IsArray<T>` 判断
- **自测**:`infer` 为什么必须写在条件类型里
- **文档**:https://www.typescriptlang.org/docs/handbook/2/conditional-types.html

### 第 19 课:声明文件 .d.ts 与 @types 机制
- **目标**:理解 `.d.ts` 的作用;知道 @types 包怎么来的;能给无类型库写最小声明
- **要点**:`.d.ts` 只描述类型不实现;`@types/*` 是 DefinitelyTyped 社区包;`declare`/`declare module`;`declare global`
- **练习**:`lessons/19-声明文件/example.d.ts` —— 给一个只有 JS 的"库"(示例函数)写最小 .d.ts;在 main.ts 里使用它
- **自测**:为什么要 declare module;`/// <reference types>` 的作用(看懂即可)
- **文档**:https://www.typescriptlang.org/docs/handbook/declaration-files/introduction.html

### 第 20 课:tsconfig 逐项解读与渐进加固策略
- **目标**:能逐项解释自己项目的 tsconfig;知道存量 `any` 代码怎么渐进收紧
- **要点**:`strict` 全家桶每个开关;`moduleResolution`/`target`/`module` 关系;`paths`/`include`/`exclude`;渐进策略:`noImplicitAny` 先开 → 新代码零 any → 用 `@ts-expect-error` 过渡
- **练习**:`lessons/20-tsconfig/` —— 把 `tsconfig.json` 的每一项抄一遍并写注释(自己的话);读工作项目(tian-ji 或 workBuddy)的 tsconfig,给 Claude 讲解每一行
- **自测**:说出 strict 全家桶 5 个以上开关及作用
- **文档**:https://www.typescriptlang.org/tsconfig

## 阶段四:Vue 3 类型实战(第 21-24 课)

> 从第 21 课起,在仓库内新建 `vue-practice/` 子目录(独立 Vite 项目,独立 tsconfig)。

### 第 21 课:Vue 3 + TS 环境与 ref/reactive/computed 类型
- **目标**:会搭 Vite + Vue3 + TS 项目;理解 `ref`/`reactive`/`computed` 的推导;知道何时显式 `Ref<T>`
- **要点**:`create-vite` 建项目;`ref(0)` 推导 `Ref<number>`;`ref<T>(null!)` 场景;`reactive` 的嵌套;`computed` 泛型
- **练习**:`vue-practice/` —— 建项目,写一个带状态管理的计数器 + 用户信息面板(全部类型推导或显式,零 any)
- **自测**:`ref<number | null>(null)` 与 `ref(null)` 的差别
- **文档**:https://cn.vuejs.org/guide/typescript/composition-api.html

### 第 22 课:defineProps/defineEmits 类型设计
- **目标**:会用类型声明定义 props/emits;能给组件设计合理的 props
- **要点**:`defineProps<{...}>()` 纯类型语法 vs 运行时语法;`withDefaults`;`defineEmits<{ (e: 'update', v: string): void }>()`;props 的命名与复用(抽 `defineProps` 类型)
- **练习**:写一个 `TodoItem.vue`(类型化 props:todo 对象、emits:删除/切换完成)
- **自测**:props 为什么推荐纯类型声明
- **文档**:同上

### 第 23 课:v-model 与 defineModel 类型
- **目标**:会用 `defineModel` 并标注类型;理解 v-model 的类型流向
- **要点**:`defineModel<string>()`;多 v-model;修饰符类型;封装可复用输入组件
- **练习**:封装 `BaseInput.vue`(v-model + 校验 props),在父组件使用并感受类型检查
- **自测**:`defineModel` 的类型参数影响什么
- **文档**:https://cn.vuejs.org/api/sfc-script-setup.html#definemodel

### 第 24 课:数据层类型实践——axios 封装与接口类型
- **目标**:会用泛型封装请求函数;会组织接口类型定义
- **要点**:`request<T>(config): Promise<T>` 封装;响应包 `{ code, data, message }` 的解包;接口类型文件组织(types/api.d.ts);错误分支类型
- **练习**:`vue-practice/` —— 写 request 封装 + 定义 2 个业务接口类型 + 在组件中使用
- **自测**:泛型 T 在 axios 封装里承担什么角色
- **文档**:无(综合应用)

## 阶段五:毕业项目(第 25-30 课)

### 第 25 课:毕业项目启动——需求与类型模型
- **目标**:独立设计项目需求与类型模型
- **项目建议**(可自选):带标签过滤的待办清单 / 记账本 / 图书管理。要求在 `vue-practice/` 中新建独立小应用
- **产出**:`README.md`(需求 + 功能清单)+ `src/types.ts`(全部业务类型:实体、状态、可辨识联合)
- **门槛**:`strict` 全开、`零 any`。类型模型先给 Claude 审阅,**审过再写代码**
- **文档**:无

### 第 26-29 课:毕业项目迭代(每课一个模块)
- 26:状态管理(组合式 store,泛型 + 类型化 state/actions)
- 27:组件层(类型化 props/emits/v-model,复用第 22-23 课能力)
- 28:数据与工具层(request 封装 + 类型守卫 + 工具函数)
- 29:收尾(补齐边缘类型、`never` 穷尽、复盘 any 记录——若有 any 必须解释并消除)
- **流程**:每课"写模块 → Claude 审 → 修";结束更新 progress.md

### 第 30 课:毕业评估
- **流程**:
  1. 逐项过 `progress.md` 里的 **20 项能力自测清单**,Claude 现场验证(让用户写/说,不是问"会不会")
  2. 错题本逐条复盘(能说出"当初为什么错、现在怎么想")
  3. 判定:全部通过 → 记录毕业日期,**B 档达成**,给出后续建议(投入工作项目拉分支升级 TS / 可选 C 档彩蛋);有未通过项 → 列出补课清单,不毕业
- **毕业奖励**:进度文件写"毕业证书"一节;建议 git commit 存档

---

## 通用说明

- **错题本**:任何一次类型错误,Claude 记入 progress.md(错误代码 + 原因 + 教训)。阶段末(14/20/24/30 课)复盘
- **any 记录**:每次出现 `any`/`as any`,Claude 当场指出并记入 progress.md。毕业时清零是硬指标
- **节奏**:单课 20-40 分钟;用户要求可多学一课;中断随时可恢复(靠 progress.md)
- **第一课启动**:用户说"学习 TS 第 1 课"时,Claude 先花 2 分钟解释"本课你要做什么",再开始讲解
